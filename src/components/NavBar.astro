---
const pathname = new URL(Astro.request.url).pathname;
const currentPath = pathname.slice(1); // remove the first "/"
---
<style>
  .bg-yellow {
    background-color: #FCF000;
  }

  nav > ul > li.active,
  nav > ul > li:hover:not(:has(ul)) {
    background-image: url(/images/black-rectangle.svg);
    background-position: 0 100%;
    background-repeat: no-repeat;
  }

  .lg\:text-xs {
    font-size: 0.85rem;
    line-height: inherit;
  }

  nav > ul > li {
    float: left;
    margin-left: 0.25rem;
    margin-right: 0.25rem;
    padding: 1.5rem 0;
  }

  @font-face {
    font-family: 'Chaney';
    src: url("/fonts/chaney-regular.woff2") format("woff2"),
      url("/fonts/chaney-regular.woff") format("woff");
    font-weight: normal;
    font-style: normal;
    font-display: swap;
  }

  .font-chaney {
    font-family: Chaney,Arial,sans-serif;
  }
</style>
<header class="max-w-screen-xl mx-auto bg-white grid grid-cols-12 pb-8">
  <div class="col-span-6 lg:col-span-4 col-start-2 lg:col-start-2 pt-4">
    <a href="/">
      <div class="font-chaney text-xs -mb-4">Open_Future</div>
      <div class="font-chaney text-3xl my-3 leading-10">Alignment Assembly</div>
    </a>
  </div>
  <div class="col-span-2 lg:col-span-7 col-start-11 lg:col-start-6">
    <nav class="bg-yellow absolute lg:static w-50 lg:w-auto top-0 right-0 lg:block h-auto lg:h-19 z-20 hidden">
      <ul class="text-md font-chaney lg:flex lg:justify-between lg:px-6 lg:text-xs xl:text-md">
        <li class:list={[ 'group', { active: currentPath === "about" } ]}><a href="/about">About Us</a></li>
        <li class:list={[ 'group', { active: currentPath === "pre-survey" } ]}><a href="/pre-survey">Pre-survey</a></li>
        <li class:list={[ 'group', { active: currentPath === "participate" } ]}><a href="/participate">Participate</a></li>
      </ul>
    </nav>
  </div>
</header>