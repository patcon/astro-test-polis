media:
  input: public/images
  output: /images
  categories: [ image ]
content:
  - name: pages
    label: Pages
    path: src/pages
    filename: "{fields.title}.mdx"
    type: collection
    fields:
      - name: title
        label: Title
        type: string
      - name: layout
        label: Layout
        type: string
